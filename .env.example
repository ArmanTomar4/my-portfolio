# Upstash Redis — visitor counter + guestbook
# Get these from https://console.upstash.com/ → your Redis database → REST API
# The site builds + runs without them; the counter shows 0 and the guestbook
# returns 503 until they're set.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
